query($cursor: String, $viewerId: ID!) {
  viewer {
    repositoriesContributedTo(
      first: 100,
      after: $cursor,
      contributionTypes: [COMMIT]
    ) {
      pageInfo {
        hasNextPage
        endCursor
      }
      nodes {
        nameWithOwner
        isArchived
        description
        primaryLanguage {
          name
          color
        }
        defaultBranchRef {
            target {
                ... on Commit {
                    historyCommitCount: history(author: {id: $viewerId}) { totalCount }
                }
            }
        }
      }
    }
  }
}