query($cursor: String, $viewerId: ID!) {
  viewer {
    repositories(
      first: 100,
      after: $cursor
    ) {
      pageInfo {
        hasNextPage
        endCursor
      }
      nodes {
        nameWithOwner
        isArchived
        description
        primaryLanguage {
          name
          color
        }
        defaultBranchRef {
            target {
                ... on Commit {
                    history(author: {id: $viewerId}) { totalCount }
                }
            }
        }
      }
    }
  }
}