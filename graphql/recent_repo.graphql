query ($viewerId: ID!) {
  viewer {
    repositories(first: 5, orderBy: {field: UPDATED_AT, direction: DESC}) {
      nodes {
        nameWithOwner
        isPrivate
        isArchived
        description
        primaryLanguage {
          name
          color
        }
        defaultBranchRef {
            target {
                ... on Commit {
                    history(author: {id: $viewerId}, first: 30) {
                       nodes {
                        committedDate
                        parents {
                          totalCount
                        }
                        deletions
                        additions
                       }
                    }
                    historyCommitCount: history(author: {id: $viewerId}) { totalCount }
                }
            }
        }
        url
      }
    }
  }
}