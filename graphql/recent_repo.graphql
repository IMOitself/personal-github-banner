query {
  viewer {
    repositories(first: 1, orderBy: {field: UPDATED_AT, direction: DESC}) {
      nodes {
        name
        isArchived
        description
        primaryLanguage {
          name
          color
        }
        updatedAt
      }
    }
  }
}