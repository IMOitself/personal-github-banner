# i got this code from https://docs.github.com/en/graphql/guides/forming-calls-with-graphql
query($number_of_repos:Int!) {
  viewer {
    name
     repositories(last: $number_of_repos) {
       nodes {
         name
       }
     }
   }
}